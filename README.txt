# Discord Event Bot

## Описание

Этот бот предназначен для автоматизации сбора и управления группами для игровых ивентов (например, MMO-рейды) с поддержкой:
- пресет-групп (до 6 человек, с лидером),
- автоматической расстановки по ролям (танк, хил, дд),
- повторяющихся и разовых событий,
- выбора каналов для анонсов,
- гибкой админ-панели для управления,
- полностью на русском языке.

---

## Возможности

- **Автоматическая регистрация участников** на событие через кнопки.
- **Выбор и сохранение роли** (танк, хил, дд) через команду и кнопки.
- **Пресет-группы**: формирование “своей” пачки через панель, назначение лидера.
- **Гибкая автосборка**: если кто-то из пресета отказывается/не может — остальные перераспределяются максимально вместе.
- **Создание разовых и повторяющихся событий** через модальные окна.
- **Выбор Discord-канала** для события.
- **Админ-панель**: через одну команду доступен полный контроль (создание/удаление событий, пресетов, лидеров и расписаний).
- **Сохранение данных** на сервере — ваши пресеты, расписания, роли не пропадут при перезапуске бота.
- **Безопасность**: только роли из списка ALLOWED_ROLES могут управлять событиями.

---

## Список команд

### Пользовательские

- `!роль <танк|хил|дд>` — Установить свою роль для автосборки.
- `!админ_панель` — Открыть административную панель (требует роль из ALLOWED_ROLES).

### Через кнопки (интерактивно в сообщении события)

- `✅ Участвовать` — Записаться в событие (роль выбирается через модальное окно, если не задана).
- `❌ Отменить` — Выйти из события.
- `🚫 Не смогу` — Отказаться от участия (вас не будут распределять в группы).

### В админ-панели (кнопки):

- `🗓 Создать событие` — Разовое событие.
- `🔁 Повторяющееся событие` — Запланировать цикл событий.
- `⚙️ Изменить повторяющееся событие` — Удалить или изменить расписание.
- `📋 Пресет-лист` — Создать пресет-группу (до 6 человек, включая себя).
- `👑 Добавить лидера` — Назначить лидеров пресет-группам.
- `✏️ Редактировать пресет-группы` — Добавить/удалить/переместить участников пресетов.

---

## FAQ

### Как записаться на событие?
- Нажмите кнопку `✅ Участвовать` под сообщением события.
- Если не выбрана роль — появится выбор (танк, хил, дд).

### Как сменить роль?
- Используйте команду:  
  `!роль танк`  
  `!роль хил`  
  `!роль дд`
- Или через кнопку-выбор при записи в событие.

### Как создать свою пресет-группу?
- Откройте панель: `!админ_панель`
- Нажмите `📋 Пресет-лист`, выберите до 6 участников (бот покажет список).
- Назначьте лидера через `👑 Добавить лидера`.

### Что если лидер ушёл из пресета?
- Остальные участники всё равно будут стараться попасть в одну группу.  
- Если мест не хватит, бот распределит максимально “вместе”.

### Как запланировать повторяющееся событие?
- Через `!админ_панель` → `🔁 Повторяющееся событие`.
- Укажите дату, время, интервал, канал — всё просто!

### Где хранятся данные?
- Все пресеты, роли и расписания хранятся в папке `guild_data/` (по каждому серверу отдельно).

### Кто может управлять событиями?
- Только пользователи с ролями из списка `ALLOWED_ROLES` (см. config.py).

---

## Архитектура и безопасность

- Все данные хранятся в отдельных JSON-файлах по каждому серверу.
- Все действия админа логируются.
- У пользователя нет доступа к чужим пресетам, если только он не админ.
- Код легко расширить под свои нужды (open source подход).

---

## Запуск

1. Установите Python 3.8+ и зависимости (discord.py, python-dotenv).
2. Создайте `.env` с переменной `DISCORD_TOKEN`.
3. Запустите:  
   `python bot.py`
4. Добавьте бота на свой сервер.

---

## Поддержка

Вопросы, баги, предложения — пишите в Discord или на GitHub проекта (если открыт репозиторий).

---
